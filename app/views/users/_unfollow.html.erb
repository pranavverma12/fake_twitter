<%= form_for(current_user.active_relationships.find_by(followed_id: user.id),
             html: { method: :delete, :style => 'background-color:transparent; padding: 0; border:0; box-shadow: none;'}) do |f| %>
  <%= f.submit "Unfollow", class: "destroyButton" %>
<% end %>