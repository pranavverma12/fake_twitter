<%= form_for(current_user.active_relationships.build, :html => { :style => 'background-color:transparent; padding: 0; border:0; box-shadow: none;'}) do |f| %>
  
  <div><%= hidden_field_tag :followed_id, user.id %></div>
  
  <%= f.submit "Follow", class: "addButton" %>
<% end %>